{
  "name": "ax3-airlines-main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd frontend && npm run dev\" \"cd backend && nodemon server\"",
    "user": "concurrently \"cd frontend && npm run dev\" \"cd backend && nodemon server\"",
    "admin": "concurrently \"cd Admin && npm run dev\" \"cd backend && nodemon server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
